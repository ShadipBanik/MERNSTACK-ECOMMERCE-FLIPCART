version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - echo "Installing dependencies..."
      - npm install
  build:
    commands:
      - echo "Building Backend..."
  post_build:
    commands:
      - echo "Updating files in the S3 bucket..."
      - npm start